#include <iostream>
using namespace std;

int main()
{
    int count, result, max;

    cin >> count; // 5

    cin >> max;
    count--;

    if (count > 0)
    {
        count--;
        cin >> result;
        if (result > max)
            max = result;
    }
    if (count > 0)
    {
        count--;
        cin >> result;
        if (result > max)
            max = result;
    }
    if (count > 0)
    {
        count--;
        cin >> result;
        if (result > max)
            max = result;
    }
    if (count > 0)
    {
        count--;
        cin >> result;
        if (result > max)
            max = result;
    }
    if (count > 0)
    {
        count--;
        cin >> result;
        if (result > max)
            max = result;
    }
    if (count > 0)
    {
        count--;
        cin >> result;
        if (result > max)
            max = result;
    }
    if (count > 0)
    {
        count--;
        cin >> result;
        if (result > max)
            max = result;
    }
    if (count > 0)
    {
        count--;
        cin >> result;
        if (result > max)
            max = result;
    }
    if (count > 0)
    {
        count--;
        cin >> result;
        if (result > max)
            max = result;
    }
}
